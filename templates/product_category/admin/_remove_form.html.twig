<form method="post" action="{{ path('admin_product_category_remove', {'id': product_category.id}) }}" onsubmit="return confirm('Voulez-vous vraiment désactiver cette catégorie ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('remove' ~ product_category.id) }}">
    <button class="btn btn-danger">Désactiver</button>
</form>
