<form method="post" action="{{ path('admin_product_category_activate', {'id': product_category.id}) }}" onsubmit="return confirm('Voulez-vous vraiment réactiver cette catégorie ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('activate' ~ product_category.id) }}">
    <button class="btn btn-success">Réactiver</button>
</form>
